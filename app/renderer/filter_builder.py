from app.core.models import Project
from app.core.timeline import Timeline


def build_filter_graph(project: Project, timeline: Timeline) -> str:
    raise NotImplementedError("FFmpeg composition is planned for a later V1 phase.")
